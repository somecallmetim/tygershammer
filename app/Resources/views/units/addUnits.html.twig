{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Add Unit</h1>

                {{ form_start(form) }}
                    {{ form_row(form.name) }}

                    <button type="submit" class="btn btn-primary" formnovalidate>
                        Add Unit
                    </button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}