{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Edit Unit</h1>

                {{ form_start(form) }}
                    {{ include('units/_unitsForm.html.twig') }}

                    <a href="{{ path('unit_delete', {'id': unit.id}) }}" class="btn btn-danger">Delete Unit</a>
                    <button type="submit" class="btn btn-primary" formnovalidate>
                        Submit Changes
                    </button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}