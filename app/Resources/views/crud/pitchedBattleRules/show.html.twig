{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Pitchedbattleruleentity</h1>

        <table>
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pitchedBattleRuleEntity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pitchedBattleRuleEntity.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pitchedBattleRuleEntity.description }}</td>
            </tr>
            </tbody>
        </table>

        <ul>
            <li>
                <a href="{{ path('pitchedBattleRules_index') }}">Back to the list</a>
            </li>
            <li>
                <a href="{{ path('pitchedBattleRules_edit', { 'id': pitchedBattleRuleEntity.id }) }}">Edit</a>
            </li>
            <li>
                {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
                {{ form_end(delete_form) }}
            </li>
        </ul>
    </div>
{% endblock %}
