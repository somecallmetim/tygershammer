{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Battlefieldrole</h1>

        <table>
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ battlefieldRole.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ battlefieldRole.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ battlefieldRole.description }}</td>
            </tr>
            </tbody>
        </table>

        <ul>
            <li>
                <a href="{{ path('battlefieldrole_index') }}">Back to the list</a>
            </li>
            <li>
                <a href="{{ path('battlefieldrole_edit', { 'id': battlefieldRole.id }) }}">Edit</a>
            </li>
            <li>
                {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
                {{ form_end(delete_form) }}
            </li>
        </ul>
    </div>
{% endblock %}
