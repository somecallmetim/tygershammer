{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Add {{ entityName }}</h1>
                {{ form_start(form) }}
                {{ include(twigForm) }}
                <button type="submit" class="btn btn-primary" formnovalidate>
                    Submit
                </button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}